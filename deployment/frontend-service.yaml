apiVersion: v1
kind: Service
metadata:
  name: traffic-frontend
  labels:
    app: traffic-control
    component: frontend
spec:
  selector:
    app: traffic-control
    component: frontend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
